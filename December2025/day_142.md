## December 28, 2025 - Day 142

### What I Worked On:
**Neural Data Science:**  
I reviewed the Exercise for Reaction Time Data in VS Code. 

### Concepts Practiced:  
- Pandas
- Lists
    
### Resources Used:  
- [Reaction Time Data](https://neuraldatascience.io/b-ai-assisted/rt-data/)
  
### Code: 
```python
rt = [0.394252808, 0.442094359, 0.534764366, 0.565906723, 0.570404592, 
      0.486154719, 0.518792127, 0.844916827, 0.495622859, 0.476159436, 
      0.612854746, 0.529661203, 0.389157455, 1.517088266, 0.573962432, 
      0.714152493, 0.409225638, 0.435308188, 0.509801957, 0.544626271, 
      0.437877745, 0.333356848, 0.401773569, 0.479840688
      ]

#What *type* of data is `rt`
type(rt)

#What *type* of data is the first value in `rt` 
type(rt[0])

#How many trials were in this experiment?
len(rt)

#Print the first 9 values in `rt`
print(rt[:9])

#Print the last 6 values in `rt`
print(rt[-6:])

#Print the values of the fifteenth through twentieth data points in `rt` (including the twentieth value)
print(rt[14:20])

#What is the *slowest* reaction time in `rt`?
max(rt)

#What is the *fastest* reaction time in `rt`?
min(rt)

#Find the index of a value
rt.index(0.409225638)

#Error Rates
err = [False, False, True, False, False, False, False, False, True, False, 
       False, True, False, False, False, False, True, True, True, False, 
       ]

#Data type of the values
type(err[0])

#How many data points
len(err)

#Insert a value at the beginning to reflect the participant's error on the first trial
err.insert(0, True)

#Insert three values at the end of err, indicating correct answers on the last three trials
err.extend([False, False, False])

#Print out the length of err
print(len(err))

#How many errors did the participant make?
err.count(True) 

#Find the position of the slowest RT in the data
slowest_index = rt.index(max(rt))

#remove the slowerst RT value from the data 
rt.remove(max(rt))

#Remove the data from err that corresponds to the slowest RT
err.pop(slowest_index)

#Print the slowest RT vlaue remaining, rounded to two decimal places 
print(round(max(rt), 2))

#Print the lengths of rt and err using a single print command with accompanying text 
print("Length of rt:", len(rt), "Length of err:", len(err))


```
